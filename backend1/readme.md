# 🧭 JagdishVCS – A Lightweight Git + GitHub Clone (CLI)

**JagdishVCS** is a command-line version control system built from scratch to replicate the essential features of **Git** and **GitHub**.  
It supports commands like `init`, `add`, `commit`, `push`, `pull`, and `revert` — all implemented using Node.js.

---

## ✨ Features

- 🏗️ Initialize repositories (`init`)
- ➕ Add files to a staging area (`add`)
- 💾 Commit files with messages (`commit`)
- ☁️ Push and Pull changes from remote (cloud)
- ⏪ Revert to any previous commit via UUID
- ⚙️ Built entirely in **Node.js**

---

## ⚙️ Installation

Install the CLI :

```bash
npm install jagdish-vcs
